schema: spec-driven

context: |
  Tech stack: TypeScript, Node.js, Commander.js, libsql, @modelcontextprotocol/sdk
  Target: AI-assisted programming tool knowledge base
  Reference architecture: GitNexus (CLI lazy-loading, RRF fusion, Skill templates, resource URIs)
  Storage: Markdown files with YAML frontmatter + libsql (SQLite with FTS5)
  No vector search (by design - latency sensitive for human dev workflow)
  Deployment: standalone CLI + MCP stdio plugin
